const arr = [];

function arrFunc(){

    testContainer.addEventListener('key')
    const label = document.createElement('label');

    const checkbox = document.createElement('input');
    checkbox.type('checkbox');

    const text = document.createElement('input');
    text.type('text');


}